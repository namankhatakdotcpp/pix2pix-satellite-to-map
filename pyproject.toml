[project]
name = "pix2pix-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "tensorflow>=2.10",
    "numpy",
    "Pillow",
    "fastapi",
    "uvicorn",
    "python-multipart",
]

[tool.vercel]
entrypoint = "api:app"
